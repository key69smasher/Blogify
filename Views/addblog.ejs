<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./particles/meta') %>
  <%- include('./particles/fonts') %>
  <title>Add Blogs - Blogify</title>
  <style>
  body {
    background-color: #121212;
    color: #e0e0e0;
  }

  .form-container {
    max-width: 700px;
    margin: 3rem auto 5rem;
    background: rgba(255 255 255 / 0.05);
    padding: 2.5rem 3rem;
    border-radius: 1rem;
    box-shadow: 0 0 25px rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(15px);
  }

  .form-container h1 {
    margin-bottom: 2rem;
    font-weight: 700;
    color: #000000;
    text-align: center;
    text-shadow: 0 0 8px rgba(0, 0, 0, 0.7);
  }

  label {
    font-weight: 600;
  }

  .form-text {
    color: #a9a9a9;
  }

  input[type="file"] {
    cursor: pointer;
  }

  .text-danger {
    font-weight: 600;
  }

  button.check{
    width: 100%;
    padding: 0.75rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 0.5rem;
    background-color: #000000;
    color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.7);
    transition: background-color 0.3s ease;
  }

  button.check:hover {
    background-color: #222222;
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.9);
  }
</style>

</head>

<body>
  <%- include('./particles/navbar') %>

  <div class="form-container">
    <h1>Add a New Blog</h1>
    <form method="post" action="/blog/addnew" enctype="multipart/form-data" novalidate>
      <div class="mb-4">
        <label for="exampleInputTitle1" class="form-label">Title</label>
        <input type="text" name="Title" class="form-control" id="exampleInputTitle1" placeholder="Enter a catchy title" required>
        <div class="form-text">Make it striking and memorable.</div>
      </div>

      <div class="mb-4">
        <label for="exampleFormControlbody1" class="form-label">Body</label>
        <textarea class="form-control" name="body" id="exampleFormControlbody1" rows="6" placeholder="Summarize your blog" required></textarea>
      </div>

      <div class="mb-4">
        <label for="exampleFormControlFile1" class="form-label">
          Upload Cover Image
          <span class="fs-6 text-dark">*formats: jpg, jpeg, png, avif, webp</span>
        </label>
        <input type="file" class="form-control" name="coverImg" id="exampleFormControlFile1" accept=".jpg,.jpeg,.png,.avif">
        <div class="form-text">Optional: Add an image to make your blog more engaging.</div>
      </div>

      <button type="submit" class="btn check">Submit Blog</button>
    </form>
  </div>
  <%- include('./particles/footer') %>
  <%- include('./particles/script') %>
</body>

</html>
